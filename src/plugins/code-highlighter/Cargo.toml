[package]
name = "code_highlighter"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
syntect = { version = "5.2.0", default-features = false, features = ["default-fancy", "html"] }

[profile.release]
lto = true
codegen-units = 1
